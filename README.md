Rating Component
This is a simple, customizable star rating component built using React. The component displays a dynamic number of stars, allows users to hover over the stars to preview the rating, and select a final rating.

Features
Dynamic number of stars (based on maxRating prop).
Hover effect to show a temporary rating (before final selection).
Clicking a star selects and locks in a rating.
Pure CSS and SVG icons for the stars.
